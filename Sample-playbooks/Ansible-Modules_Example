Example 1:
ansible demo -b -m yum -a "pkg=httpd state=present"


# demo is the group name of managed node
  -b used for providing sudo privilege
  -m states that it is a module
  -a is for arguments
  pkg is for stating package to be considered
  
  state---  
    present -> install
    absent -> uninstall
    update -> latest version
